name: "core_dio"

version: "1.0.0"

resolution: workspace

dependencies:
  # Версии должны быть указаны точно, без диапазонов
  core_failure: any
  core_result: any
  dio: "5.9.2"
  freezed_annotation: "3.1.0"

dev_dependencies:
  # Версии должны быть указаны точно, без диапазонов
  build_runner: "2.12.2"
  freezed: "3.2.5"

environment:
  sdk: "3.11.1"

publish_to: "none"
